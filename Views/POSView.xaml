<UserControl x:Class="WpfApp1.Views.POSView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             Background="Transparent" d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#F8F5F0" BorderBrush="#D4C4B7" BorderThickness="1" Margin="0,0,10,0" CornerRadius="8" Padding="15">
            <StackPanel>
                <TextBlock Text="Selecciona una Mesa" FontSize="18" FontWeight="SemiBold" Foreground="#1A1514" Margin="0,0,0,15"/>

                <WrapPanel x:Name="PanelMesas" Orientation="Horizontal">
    
                </WrapPanel>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Background="#FFFFFF" BorderBrush="#D4C4B7" BorderThickness="1" CornerRadius="8" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Mesa Seleccionada: Ninguna" FontSize="16" FontWeight="Bold" Foreground="#1A1514" Margin="0,0,0,10"/>

                <ListBox Grid.Row="1" Background="#F8F5F0" BorderBrush="#D4C4B7" BorderThickness="1" Margin="0,10" Foreground="#1A1514" FontSize="14">
                    <ListBoxItem Content="1x Catan (Juego) - $5.00"/>
                    <ListBoxItem Content="2x Frappe Oreo - $7.00"/>
                </ListBox>

                <StackPanel Grid.Row="2" Margin="0,10,0,0">
                    <TextBlock Text="Total: $12.00" FontSize="22" FontWeight="Bold" Foreground="#1A1514" HorizontalAlignment="Right" Margin="0,0,0,15"/>
                    <Button Content="💵 COBRAR Y ABRIR CAJA" Height="50" Background="#1A1514" Foreground="#EFEBE4" FontSize="16" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>