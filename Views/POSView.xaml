<UserControl x:Class="WpfApp1.Views.POSView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             Background="Transparent" d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,0,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#F8F5F0" BorderBrush="#D4C4B7" BorderThickness="1" CornerRadius="8" Padding="15">
                <StackPanel>
                    <TextBlock Text="Selecciona una Mesa" FontSize="18" FontWeight="SemiBold" Foreground="#1A1514" Margin="0,0,0,10"/>
                    <WrapPanel x:Name="PanelMesas" Orientation="Horizontal">
                    </WrapPanel>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Background="#F8F5F0" BorderBrush="#D4C4B7" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,15,0,0">
                <StackPanel>
                    <TextBlock Text="Menú Rápido (Juegos y Cafetería)" FontSize="18" FontWeight="SemiBold" Foreground="#1A1514" Margin="0,0,0,10"/>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Height="300">
                        <WrapPanel x:Name="PanelProductos" Orientation="Horizontal">
                        </WrapPanel>
                    </ScrollViewer>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Column="1" Background="#FFFFFF" BorderBrush="#D4C4B7" BorderThickness="1" CornerRadius="8" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="lblMesaSeleccionada" Grid.Row="0" Text="Mesa Seleccionada: Ninguna" FontSize="16" FontWeight="Bold" Foreground="#1A1514" Margin="0,0,0,10"/>

                <ListBox x:Name="ListaTicket" Grid.Row="1" Background="#F8F5F0" BorderBrush="#D4C4B7" BorderThickness="1" Margin="0,10" Foreground="#1A1514" FontSize="14"/>

                <Button x:Name="btnQuitarProducto" Grid.Row="2" Content="🗑️ Quitar Seleccionado" Height="35" Background="#FF5252" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Margin="0,0,0,15" Click="BtnQuitarProducto_Click"/>

                <StackPanel Grid.Row="3" Margin="0,10,0,0">
                    <TextBlock x:Name="lblTotal" Text="Total: $0.00" FontSize="22" FontWeight="Bold" Foreground="#1A1514" HorizontalAlignment="Right" Margin="0,0,0,15"/>

                    <Button x:Name="btnCobrar" Content="💵 COBRAR Y ABRIR CAJA" Height="50" Background="#1A1514" Foreground="#EFEBE4" FontSize="16" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="BtnCobrar_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>