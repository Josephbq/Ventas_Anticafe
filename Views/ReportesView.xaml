<UserControl x:Class="WpfApp1.Views.ReportesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Background="Transparent" d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="0,0,10,0">
        <Border Background="#F8F5F0" BorderBrush="#D4C4B7" BorderThickness="1" CornerRadius="8" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBlock Text="Ventas del Día:" FontSize="18" FontWeight="Bold" Foreground="#1A1514" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <DatePicker x:Name="dpFecha" Width="150" Height="30" SelectedDateChanged="DpFecha_SelectedDateChanged"/>
                </StackPanel>

                <DataGrid x:Name="GridReporte" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                          Background="White" BorderBrush="#D4C4B7" RowHeight="35" HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Ticket Nº" Binding="{Binding IdVenta}" Width="80"/>
                        <DataGridTextColumn Header="Hora" Binding="{Binding FechaVenta}" Width="150"/>
                        <DataGridTextColumn Header="Mesa" Binding="{Binding IdMesa}" Width="80"/>
                        <DataGridTextColumn Header="Total Pagado ($)" Binding="{Binding TotalPagado}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <Grid Grid.Row="2" Margin="0,15,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="btnExportarWord" Grid.Column="0" Content="📄 Exportar a Word (.doc)" Height="45" Width="200" Background="#2B579A" Foreground="White" FontSize="14" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="BtnExportarWord_Click"/>

                    <TextBlock x:Name="lblTotalDia" Grid.Column="1" Text="Total del Día: $0.00" FontSize="22" FontWeight="Bold" Foreground="#1A1514" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>