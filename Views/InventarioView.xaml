<UserControl x:Class="WpfApp1.Views.InventarioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Background="Transparent" d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="0,0,10,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#F8F5F0" BorderBrush="#D4C4B7" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,15,0">
            <StackPanel>
                <TextBlock Text="Registrar Producto" FontSize="20" FontWeight="Bold" Foreground="#1A1514" Margin="0,0,0,20"/>

                <TextBlock Text="Nombre del Producto:" FontWeight="SemiBold" Foreground="#1A1514" Margin="0,0,0,5"/>
                <TextBox x:Name="txtNombre" Height="35" FontSize="14" Padding="5" Margin="0,0,0,15" BorderBrush="#D4C4B7"/>

                <TextBlock Text="Categoría (Tipo):" FontWeight="SemiBold" Foreground="#1A1514" Margin="0,0,0,5"/>
                <ComboBox x:Name="cmbTipo" Height="35" FontSize="14" Padding="5" Margin="0,0,0,15" BorderBrush="#D4C4B7">
                    <ComboBoxItem Content="CAFETERIA" IsSelected="True"/>
                    <ComboBoxItem Content="LUDOTECA"/>
                </ComboBox>

                <TextBlock Text="Precio de Venta ($):" FontWeight="SemiBold" Foreground="#1A1514" Margin="0,0,0,5"/>
                <TextBox x:Name="txtPrecio" Height="35" FontSize="14" Padding="5" Margin="0,0,0,25" BorderBrush="#D4C4B7"/>

                <Button x:Name="btnGuardar" Content="💾 GUARDAR PRODUCTO" Height="45" Background="#8C5A35" Foreground="#EFEBE4" FontSize="14" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="BtnGuardar_Click"/>

                <TextBlock x:Name="lblMensaje" Foreground="#4CAF50" FontWeight="SemiBold" Margin="0,15,0,0" TextAlignment="Center" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Background="#FFFFFF" BorderBrush="#D4C4B7" BorderThickness="1" CornerRadius="8" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Catálogo de Productos Activos" FontSize="20" FontWeight="Bold" Foreground="#1A1514" Margin="0,0,0,15"/>

                <DataGrid x:Name="GridProductos" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                          Background="White" BorderBrush="#E0E0E0" RowHeight="35" HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding IdProducto}" Width="50"/>
                        <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                        <DataGridTextColumn Header="Categoría" Binding="{Binding Tipo}" Width="120"/>
                        <DataGridTextColumn Header="Precio ($)" Binding="{Binding PrecioVentaBase}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>